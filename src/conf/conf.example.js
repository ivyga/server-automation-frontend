// An example of the conf.js file (that file is not in github but setup locally and/or on the web server).
export default {
    environment: 'local',
    backendUrl: 'https://datausa.io/api/',
    shouldMockBackend: false,
};
